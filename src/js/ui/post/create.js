// import FormHandler from "../auth/register.js";
// import { createPost } from "../../api/post/create.js";

// FormHandler.initialize("#createPostForm", (event, form) =>
//   FormHandler.handleSubmit(event, form, createPost)
// ); // Anouther place maybe??

// import { createPost } from "../../api/post/create";

// export async function onCreatePost(event) {
//   event.preventDefault();

//   // Extract form data
//   const form = event.target;
//   const title = form.elements["title"].value; // Assuming you have an input with name="title"
//   const body = form.elements["body"].value; // Assuming you have an input with name="body"
//   const tags = form.elements["tags"].value.split(",").map((tag) => tag.trim()); // Assuming you have an input with name="tags"
//   const mediaUrl = form.elements["media-url"].value; // Assuming you have an input with name="media-url"
//   const mediaAlt = form.elements["media-alt"].value; // Assuming you have an input with name="media-alt"
// }
